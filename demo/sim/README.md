# demo/sim — Gazebo simulation placeholder

**Not implemented in v0.1.** This directory is reserved for v0.2 work:

- Hetzner CX32 (CPU-only, ¥80/month) or equivalent Linux VM
- ROS 2 Humble + Gazebo Harmonic + `hello-robot/stretch_ros2`
- `aws-robotics/aws-robomaker-hospital-world` SDF ported to Harmonic
- "drink-water" action script (Nav2 → MoveIt 2 → return)
- `gz sim --headless-rendering` + `ffmpeg` → 1080p MP4 clips
- Clips shipped as static assets in `demo/web/public/videos/`

See `planning/15-3d-sim-decision.md` and the v0.2 section of the root
`README.md` for the full plan.
